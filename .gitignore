# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# generated feature matrices (LFS push blocked on proxy; regenerate locally)
features/train_features_v2*.npz
features/train_features_v3*.npz
features/train_features_v4*.npz
features/train_features_v5*.npz
features/train_raw_stream.npz

# cached VAL logits for blend evaluation (regenerable from eval_base/train_seq)
features/val_base_logits.npz
features/val_seq_logits*.npz

# local test artifacts
submission/_model/
submission/_model_smoke/

# crunch CLI local state — contains an AUTH TOKEN (secret). Never commit.
# NOTE: token + project.json were committed in earlier history; rotate the token
# and purge from history in round 6 (see context.md "Security remediation").
.crunchdao/
**/.crunchdao/
